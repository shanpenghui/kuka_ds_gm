#include "ros/ros.h"
#include <fstream>
using namespace std;

int main(int argc,char** argv)
{
  /*ofstream output("/home/edward/kuka_ws/out.txt");
  output<<"2018"
   output.close();*/
  /*ifstream in("/home/edward/kuka_ws/out.txt");
  
  double buffer[200];
  in>>buffer;
  a=buffer;
  ROS_INFO("%d", a); 
  if(in.is_open())
  {
    while(!in.eof())
    {
        in.getline(buffer,100);
      //ROS_INFO("%d", buffer); 
        in.close();
    }
   }*/
  return 0;
}

